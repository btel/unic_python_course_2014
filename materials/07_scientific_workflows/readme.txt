This is a full workflow for analysing neuronal correlations. You can generate the input files (optional) using the generate_data.py script. 

There are two options to run the analysis pipeline:

1) Through bash script. In the command line call:

   sh run_pipeline.sh

2) Build automation tool. Install doit[1] with:

   pip install doit

and then call:

   doit

